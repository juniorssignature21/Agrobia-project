# from django.urls import path
# from .views import OrderListCreate, OrderDetail, BuyerSignupView, BuyerLoginView, CartDetailView, AddToCartView
# from . import views

# urlpatterns = [
#     path('orders/', OrderListCreate.as_view(), name='order-list-create'),
#     path('orders/<int:pk>/', OrderDetail.as_view(), name='order-detail'),
#     path('signup/', BuyerSignupView.as_view(), name='buyer-signup'),
#     path('login/', BuyerLoginView.as_view(), name='buyer-login'),
#     path('cart/', CartDetailView.as_view(), name='cart-detail'),
#     path('cart/add/', AddToCartView.as_view(), name='add-to-cart'),
# ]
